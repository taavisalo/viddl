[project]
name = "viddl"
version = "0.1.0"
description = "CLI utility to download videos"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
  { name = "Taavi" }
]
dependencies = [
  "curl-cffi>=0.10,<0.14",
  "yt-dlp>=2024.08.06",
]

[project.scripts]
viddl = "viddl.cli:main"

[build-system]
requires = ["hatchling>=1.20.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["viddl"]
